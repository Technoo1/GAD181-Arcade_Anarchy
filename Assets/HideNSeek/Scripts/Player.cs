using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerControls : MonoBehaviour

//key press to peek from cover
//key press to hide (different from peeking key)
//ARCADE MACHINE button press(es) to do same as above
//swap out peeking and hiding sprites with each other per respective key press
//key press to collect Intel, and only when player is peeking
{
    // Update is called once per frame
    void Update()
    {

    }
}
